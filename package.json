{
  "name": "ts-solver",
  "version": "1.1.0",
  "description": "Turnstile Solver - Browser pool based Cloudflare bypass",
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun src/tests/test.ts",
    "build": "bunx @vercel/ncc build src/index.ts -o dist --target es2022"
  },
  "dependencies": {
    "chrome-launcher": "^1.1.2",
    "express": "^5.1.0",
    "ghost-cursor": "^1.3.0",
    "puppeteer-extra": "^3.3.6",
    "rebrowser-puppeteer-core": "^23.3.1",
    "tree-kill": "^1.2.2",
    "xvfb": "^0.4.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "typescript": "^6.0.2"
  }
}
